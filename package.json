{
  "name": "securegate-777g",
  "version": "1.0.0",
  "type": "module",
  "description": "SecureGate 777G - EIP-777G Genesis Gate Dashboard",
  "private": true,
  "scripts": {
    "dev": "node server.js",
    "check": "node --check live/js/app.js && node --check live/js/gate.js && node --check api/relay.js && node --check api/bypass-verify.js",
    "test": "npm run check",
    "obfuscate": "node scripts/obfuscate.js",
    "vercel-build": "npm run obfuscate",
    "final-check": "node scripts/final-check.cjs",
    "live-stale-check": "node scripts/live-stale-check.cjs"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "cors": "^2.8.6",
    "ethers": "^6.13.0",
    "express": "^5.2.1",
    "playwright": "^1.61.0"
  },
  "devDependencies": {
    "javascript-obfuscator": "^4.1.1",
    "terser": "^5.31.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
