{
  "name": "securegate-777g",
  "version": "1.0.0",
  "description": "SecureGate 777G - EIP-777G Genesis Gate Dashboard",
  "private": true,
  "scripts": {
    "check": "node --check live/js/genesis-verification.js && node --check live/js/app.js && node --check live/api/deploy/funding.js && node --check live/api/deploy/[chain].js && node --check live/api/relay.js",
    "test": "npm run check",
    "obfuscate": "node scripts/obfuscate.js",
    "vercel-build": "npm run obfuscate",
    "build:live": "cp -r live _live && echo 'Build complete: _live directory ready for deployment'"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "javascript-obfuscator": "^4.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
