{
  "name": "securegate-777g",
  "version": "1.0.0",
  "description": "SecureGate 777G - EIP-777G Genesis Gate Dashboard",
  "private": true,
  "scripts": {
    "check": "node --check live/js/app.js && node --check api/relay.js && node --check api/bypass-verify.js",
    "test": "npm run check",
    "obfuscate": "echo 'Skipping obfuscation for production'",
    "vercel-build": "echo 'Skipping obfuscation for production'",
    "build:live": "cp -r live _live && echo 'Build complete: _live directory ready for deployment'"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "terser": "^5.31.0",
    "javascript-obfuscator": "^4.1.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
