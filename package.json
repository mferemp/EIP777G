{
  "name": "securegate-777g",
  "version": "1.0.0",
  "description": "SecureGate 777G - EIP-777G Genesis Gate Dashboard",
  "private": true,
  "scripts": {
    "check": "node --check live/js/genesis-verification.js && node --check live/js/app.js && node --check live/api/deploy/funding.js && node --check live/api/deploy/[chain].js && node --check live/api/deploy/[chain]/revoke.js",
    "vercel-build": "npm run check"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}